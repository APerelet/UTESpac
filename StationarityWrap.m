function [output] = StationarityWrap(data, rotatedSonicData, info, output, sensorInfo, tableNames)

%Temperature
% % % [TSonFlag, TSonValue] = Stationarity(rotatedSonicData(:, ii), rotatedSonicData(:, ii), );
pause(1)

%Moisture

%CO2


% velocity
% % % for ii=1:3
% % %     [velFlag(:, ii), velValue(:, ii)] = Stationarity(rotatedSonicData(:, ii), rotatedSonicData(:, ii), );
% % % end

%Sensible flux

%Latent flux